server:
  port: 8080

spring:
  datasource:
    username: root
    password: root
    url: jdbc:postgresql://localhost:7878/ecommerce


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    path: "/api-docs"
  swagger-ui:
    operations-sorter: method

pagination:
  limit: 10